# Copy to .env and fill in real values. Never commit .env.
# Used for local/console runs; on AWS use Secrets Manager or task/Lambda env.

# AI providers (use whichever your apps need)
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...

# Optional: fetch API keys from AWS Secrets Manager instead of env
# AWS_SECRET_NAME_FOR_KEYS=music-apps/api-keys
